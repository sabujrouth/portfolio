/* This JavaScript code is using the Typed.js library to create a typing animation effect on an element
with the class "text". The `Typed` constructor is being used to initialize a new instance of the
Typed object with the following configuration options: */
var typed = new Typed(".text", {
    strings: ["Froentend Developer" , "YouTuber" , "Web Developer" , "Graphic Designer"],
    typeSpeed: 100,
    backSpeed: 100,
    backDelay: 1000,
    loop:true
});
